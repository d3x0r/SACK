
add_program( scan_ports ${FIRST_GCC_PROGRAM_SOURCE} 
scanner 
${LAST_GCC_PROGRAM_SOURCE}
)
SET_TARGET_PROPERTIES(scan_ports PROPERTIES FOLDER "Network Examples" )


if( NOT __ARM__ )

add_program( scan_addresses ${FIRST_GCC_PROGRAM_SOURCE} 
      pinger 
${LAST_GCC_PROGRAM_SOURCE}
)
SET_TARGET_PROPERTIES(scan_addresses PROPERTIES  FOLDER "Network Examples")


endif( NOT __ARM__ )

add_program( nameof ${FIRST_GCC_PROGRAM_SOURCE} 
      nameof 
${LAST_GCC_PROGRAM_SOURCE}
)
SET_TARGET_PROPERTIES(nameof PROPERTIES		FOLDER "Network Examples" )

add_program( ipof ${FIRST_GCC_PROGRAM_SOURCE} 
    ipof 
  ${LAST_GCC_PROGRAM_SOURCE}
)
SET_TARGET_PROPERTIES(ipof PROPERTIES FOLDER "Network Examples" )


string( REPLACE ";" " " PLATFORM_LIBRARIES_ARG "${SACK_PLATFORM_LIBRARIES}" )
string( REPLACE ";" " " PLATFORM_DEFINES_ARG "${PLATFORM_DEFINES}" )

ExternalProject_Add( scan_portable 
	SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/portable
	BINARY_DIR scan_portable
	INSTALL_DIR ${CMAKE_INSTALL_PREFIX}
	CMAKE_ARGS -DCMAKE_INSTALL_PREFIX:PATH=<INSTALL_DIR> 
		-DCMAKE_BUILD_TYPE:PATH=${CMAKE_BUILD_TYPE} 
		-DSACK_BASE=${SACK_BASE}
		-DPLATFORM_LIBRARIES=${PLATFORM_LIBRARIES_ARG}
		-DPLATFORM_DEFINES=${PLATFORM_DEFINES_ARG}
		-DSHA1_SOURCES=${SHA1_SOURCES}
		-DSHA2_SOURCES=${SHA2_SOURCES}
)

#add_subdirectory( portable )

