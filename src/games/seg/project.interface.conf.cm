
# default to using opengl interface
option default sack/vidlib/use interface = opengl
# default 360 view to 0, which will default to open on screen 0 fullscreen
option default SACK/Video Render/360 view = 0

include "@/interface.conf"

module ${CMAKE_SHARED_LIBRARY_PREFIX}${PROJECT_NAME}${CMAKE_SHARED_LIBRARY_SUFFIX}
